#include <stdio.h>
#define SIZE 10

int hashTable[SIZE];

void insert(int key) {
    int index = key % SIZE;
    while(hashTable[index] != 0)
        index = (index + 1) % SIZE;
    hashTable[index] = key;
}
void display() {
    for(int i=0;i<SIZE;i++)
        printf("%d -> %d\n", i, hashTable[i]);
}
int main() {
    int keys[] = {23, 43, 13, 27};
    for(int i=0;i<4;i++) insert(keys[i]);
    display();
    return 0;
}
